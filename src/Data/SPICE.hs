module Data.SPICE
    ( Kernel, comment, load
    , Body (..), locate
    )
where

-- spk -----------------------------------------------------------------------
import           Data.SPICE.Body (Body (..), locate)
import           Data.SPICE.Kernel (Kernel, comment, load)

